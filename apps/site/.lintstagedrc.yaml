# Frontend: форматирование изменённых файлов + быстрый линт кода.
# Полный eslint прогоняется в pre-push (`task lint`) и CI — он медленнее.
'**/*.{ts,vue}':
  - bunx oxfmt
  - bunx oxlint --fix
'**/*.{css,json,yaml,yml,md}':
  - bunx oxfmt
