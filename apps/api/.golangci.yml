version: "2"

run:
  timeout: 5m

linters:
  default: standard
  enable:
    - sloglint
  exclusions:
    generated: lax
    rules:
      - path: _test\.go
        linters:
          - errcheck
          - gosec

formatters:
  enable:
    - gofmt
    - goimports
