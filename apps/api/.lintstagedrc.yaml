# Go: форматирование + линт всего модуля.
# golangci-lint работает на уровне пакетов, поэтому пути файлов игнорируются
# (см. scripts/lint-staged-golangci.sh) — прогоняется весь модуль.
"**/*.go":
  - gofmt -w
  - ../../scripts/lint-staged-golangci.sh
